import { MandatoryFieldsPipe } from './mandatory-fields-pipe.pipe';

describe('MandatoryFieldsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MandatoryFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
